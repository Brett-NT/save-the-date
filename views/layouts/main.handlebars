<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Save The Date</title>
<link rel="stylesheet" href="../../public/stylesheets/styles.css">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
</head>

<body>
    <div class="wrapper">
        <div class="navbar is-info level">
                <div class="navbar-brand navbar-item p-5 pl-6">
                    <h1>
                        <a class="title is-1 has-text-centered has-text-white" href="/">Save The Date</a>            
                    </h1>
                    <div class="p-3">
                    <img  src="https://www.clipartmax.com/png/full/31-316035_calendar-icon-calendar-icon.png" alt="alternatetext">
                    </div>
                </div> 
                <div class="navbar-brand navbar-item"> 
                    <nav class="pr-5">
                        {{#if loggedIn}}
                        <a class="title is-4 p-4 has-text-white" href="/dashboard">Dashboard</a>
                        <button id="logout" class="btn-no-style has-text-black">Logout</button>
                        {{else}}
                        <a class="has-text-white title is-4" href="/login">Login</a>
                        {{/if}}
                    </nav>
                </div> 
        </div>
        <main>
            {{{ body }}}
        </main>
        <footer class="">
            <div class="content has-text-centered">
                <p class="has-background-info has-text-white is-size-4 p-4">Let's Get To Planning!</p> 
            </div>
        </footer>
    </div>
    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
</body>

</html>